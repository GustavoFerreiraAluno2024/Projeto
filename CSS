/* Estilo global */
body {
    color: white;
    background: black;
    margin: 0;
    font-family: "Chakra Petch", sans-serif;
    margin-bottom: 100px;
}

/* Estilo do cabeçalho */
header {
    border-bottom: solid 2px rgb(42, 122, 228);
    padding: 20px;
    font-size: 32px;
    color: rgb(42, 122, 228);
    text-align: center; /* Centraliza o texto */
}

/* Seção de chamada principal */
.chamada {
    background: rgb(184, 156, 213);
    padding: 80px 0;
    display: flex;
    justify-content: center;
    align-items: center; /* Alinha os itens verticalmente */
    text-align: center; /* Centraliza o texto */
    flex-wrap: wrap; /* Permite que o conteúdo se ajuste em telas menores */
}

/* Estilo do texto de chamada */
.chamada-texto {
    margin-right: 5%;
}

h1 {
    font-size: 40px;
    margin-bottom: 10px; /* Adiciona um espaçamento inferior */
}

p {
    font-size: 20px;
}

/* Contêiner de vídeo */
.video-container {
    max-width: 100%; /* Responsivo para telas menores */
}

/* Seção de categoria */
.categoria {
    padding: 0 20px;
    margin-top: 50px;
}

/* Estilo dos vídeos na seção de categoria */
.categoria-videos {
    display: flex;
    overflow-x: auto;
    gap: 10px;
    padding: 20px 0; /* Adiciona espaçamento */
}

.categoria-videos img {
    opacity: 0.5;
    height: 200px;
    transition: opacity 0.3s ease, border 0.3s ease; /* Adiciona transição suave */
}

/* Estilo ao passar o mouse sobre as miniaturas */
.categoria-videos img:hover {
    opacity: 1.0;
    border: 3px solid green;
}

/* Estilo dos títulos de categoria */
.categoria h2 {
    color: rgb(42, 122, 228);
    text-align: center; /* Centraliza o texto */
}
